"use client";
import { FormEvent, useState } from "react";
export function AdminLoginForm() { const [error, setError] = useState(""); const [loading, setLoading] = useState(false); async function login(event: FormEvent<HTMLFormElement>) { event.preventDefault(); setLoading(true); setError(""); const data = Object.fromEntries(new FormData(event.currentTarget)); const response = await fetch("/api/admin/session", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify(data) }); if (response.ok) window.location.assign("/admin"); else { const body = await response.json(); setError(body.error || "登录失败。"); setLoading(false); } } return <form onSubmit={login} className="admin-form"><h1>GRIMM PUMP</h1><p>南美网站中文管理后台</p><label>账号<input name="email" type="email" required autoComplete="username" /></label><label>密码<input name="password" type="password" required autoComplete="current-password" /></label><button className="btn" disabled={loading}>{loading ? "正在登录…" : "登录后台"}</button>{error && <p className="form-error">{error}</p>}</form>; }
