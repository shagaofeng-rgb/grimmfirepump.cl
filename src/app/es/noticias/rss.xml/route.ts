import { getDefaultNewsSite, getPublicNews } from "@/lib/industry-news";
export const dynamic = "force-dynamic";
const escapeXml = (value: unknown) => String(value).replace(/[<>&'\"]/g, (character) => ({ "<": "&lt;", ">": "&gt;", "&": "&amp;", "'": "&apos;", '"': "&quot;" })[character] || character);
export async function GET() { const site = await getDefaultNewsSite(); const articles = site ? await getPublicNews(site.siteId) : []; const body = `<?xml version="1.0" encoding="UTF-8"?><rss version="2.0"><channel><title>${escapeXml(site?.brandName || "Noticias")}</title><link>${escapeXml(site ? `${site.siteUrl}${site.listRoute}` : "")}</link><description>Noticias verificadas del sector</description>${articles.map((article) => `<item><title>${escapeXml(article.title)}</title><link>${escapeXml(article.canonical_url)}</link><guid>${escapeXml(article.canonical_url)}</guid><pubDate>${new Date(String(article.published_at)).toUTCString()}</pubDate><description>${escapeXml(article.deck)}</description></item>`).join("")}</channel></rss>`; return new Response(body, { headers: { "content-type": "application/rss+xml; charset=utf-8", "cache-control": "no-store" } }); }
